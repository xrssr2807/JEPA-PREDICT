# ICASSP 2027 实验进度表

最后更新：2026-07-29

| 编号 | 实验 | 状态 | 结果状态 | 下一步 |
|---|---|---|---|---|
| DATA-01 | 八标签患者级划分与泄漏审计 | 已完成 | 审计通过、划分冻结 | 保留划分 SHA256 |
| P1-01 | SP 开关 × PPG/ECG/双通道，三随机种子 | 已完成 | 18/18 完成、测试集封存 | 保留冻结材料 |
| ABL-01 | 核心预训练与架构消融 | seed 42 完成 7 项 | 完整 Phase 2 冠心病 AUC 0.7940；Transport-off 0.7798；测试集封存 | 复现 Phase 2、MIL off、multi-scale off、Transport-off 的 seed 3407/2026 |
| XAI-01 | Transport 生理与因果方向解释 | seed 42 严格机制验证已完成 | 512 片段/457 位患者；方向、顺序和配对对照成立；动态效应较小；PAT 数值一致性不成立；测试集封存 | 补齐 Transport-on/off seed 3407/2026 |

## XAI-01 冒烟归档（2026-07-29）

`results/smoke_seed42` 已保存控制台日志、患者/片段级传输指标、图、报告、脚本、输入权重与冻结划分哈希。运行仅使用验证集：512/512 输出文件属于 `val`，训练集与测试集匹配均为 0。动态 Transport 优于固定、零/负时延、反转和错配对照，但 PAT Spearman 仅 `0.1140`、MAE `184.3 ms`，冠心病时延差的 95% CI 跨零；不可进入主表或支撑因果/疾病关联表述。源代码未提交且完整启动命令未保留，后续完整运行须修复这些可复现性缺口。
| BASE-01 | 监督与自监督基线 | 尚未开始 | 尚未运行 | 固定实现与预算 |
| STAT-01 | 患者级置信区间与显著性检验 | 尚未开始 | 缺患者级预测 | 后续运行必须保存患者级预测 |
| TEST-01 | 一次性封存测试集评估 | 已锁定 | 未使用 | SELECT/ABL/BASE 冻结后执行 |

## XAI-02 严格 128 片段冒烟归档（2026-07-29）

`results/strict_smoke_seed42` 已保存日志、患者/片段级传输指标、图、报告及结果哈希。日志以 `[Complete]` 结束，仅使用冻结验证集且 `test_set_sealed=true`；128 个片段对应 124 个 UID，与患者表 UID 完全一致。动态 Transport 优于固定/零/负时延、反转和错配对照，但 PAT Spearman 仅 `0.0495`、MAE `254.4 ms`，冠心病时延差 CI `[-1.2, 0.3]` 跨零。NaN 仅来自无有效 PAT 和不可定义的跨片段方差派生字段。该结果仅为质控，不得进入主表或支撑因果/疾病关联表述；未跟踪脚本和未持久化完整命令仍是必须修复的可复现性缺口。

## XAI-03 严格 457 患者机制验证（2026-07-29）

`results/strict_seed42` 覆盖冻结验证集 512 个片段、457 位患者，并完成患者级
bootstrap。时间反转、负时延、token 策略打乱和跨患者波形错配均显著劣于
动态正向 Transport。segment-static 对照差值为 `+0.000041`，95% CI
`[0.000010, 0.000081]`，说明 token 动态效应存在但效应量很小。模型时延与
PAT 的 Spearman 为 `0.0224`、MAE 为 `256.2 ms`，不可解释为 PAT/PTT
恢复。测试集保持封存。代码、运行脚本、完整输出和输入路径已在实验目录归档。

## P2 归档检查（2026-07-28）

完整 Phase 2、MIL off、multi-scale off 均正常 early stop 完成。三项均使用 Git `ee0a0e095a8825c0b02bf4f7fa2ca72e8e3b9d6c`、冻结划分 SHA256 `e3d458a8e88c75bf0b144be9bca5c7ecc87173f75425fe5cf0e2d77822cb9716` 与 seed 42。UID 审计的 train/val/test 为 5387/1155/1155 且互斥；日志未见 NaN/Inf、错误或中断，并以 `TEST SEALED` 结束。

完整 Phase 2 相对 random-init 的冠心病 AUC 增加 0.0398；关闭 MIL 后下降 0.0069；关闭 multi-scale 后增加 0.0017，但宏 AUC 略降 0.0003。逐疾病指标和患者级预测未输出，单种子结果没有推断性置信区间，不纳入主表。

测试集在核心消融和基线冻结前不得解封。

## P2 Transport-off 归档（2026-07-28）

Transport-off（seed 42）已完成归档：Phase 2 无 Transport 预训练在最佳验证损失 `0.000883` 后正常 early stop；下游开发集训练正常结束，宏 AUC `0.7803`、冠心病 AUC `0.7798`、宏 F1 `0.3322`。相对完整 Phase 2，冠心病 AUC 为 `-0.0142`、宏 AUC 为 `+0.0001`、宏 F1 为 `-0.0145`。使用同一冻结划分与 Git `ee0a0e095a8825c0b02bf4f7fa2ca72e8e3b9d6c`；日志无 NaN/Inf、错误或中断，并以 `TEST SET SEALED` 结束。未生成逐疾病结果或患者级预测，因此无配对置信区间；单种子结果不得进入主表或触发测试集访问。
